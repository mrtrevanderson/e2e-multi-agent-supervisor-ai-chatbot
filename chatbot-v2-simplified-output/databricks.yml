bundle:
  name: databricks-chatbot-v2

variables:
  serving_endpoint_name:
    description: "Name of the model serving endpoint to be used by the app"
    default: "mas-05f2890c-endpoint"
  resource_name_suffix:
    description: "Suffix for resource names (app names etc). Helpful for avoiding name collisions if multiple users deploy this app to the same workspace"

resources:
  apps:
    databricks_chatbot:
      # NOTE: customize the app name here, if desired
      name: db-chatbot-${var.resource_name_suffix}
      description: "Agentic Chat v2 — simplified output (no tool call UI)"
      source_code_path: .
      resources:
        - name: serving-endpoint
          description: "Databricks serving endpoint name for the AI agent"
          serving_endpoint:
            name: ${var.serving_endpoint_name}
            permission: CAN_QUERY

targets:
  dev:
    mode: development
    default: true
    variables:
      resource_name_suffix: dev-v2-${workspace.current_user.domain_friendly_name}

  staging:
    mode: production
    variables:
      resource_name_suffix: "v2-staging"

  prod:
    mode: production
    variables:
      resource_name_suffix: "v2-prod"
