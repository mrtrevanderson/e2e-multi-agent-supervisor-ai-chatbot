# On Windows, npm workspace symlinks require Developer Mode to be enabled.
# If you see EISDIR errors during npm install, enable it:
#   Settings → Privacy & Security → For Developers → Developer Mode → On
# Then re-run: npm install
#
# Alternatively, run your terminal as Administrator.
# This is a Windows-only limitation; Linux/CI installs succeed without any changes.
